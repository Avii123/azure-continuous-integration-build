pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Modifying temp.txt ...'
                echo "adding a new line" >> temp.txt
                git add .
                git commit -m "added artifact"
                git push
            }
        }
    }
}